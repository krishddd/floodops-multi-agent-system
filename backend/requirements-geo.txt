# Heavy geospatial stack — only needed by the raster/vector connectors
# (ECMWF/Sentinel Hub/HydroSHEDS/WorldPop/GLIMS/Dartmouth). Requires system GDAL;
# install in a fat image only. The keyless connectors do NOT need these.
#   pip install -r requirements.txt -r requirements-geo.txt
dataretrieval>=1.0.0
xarray>=2024.0.0
geopandas>=1.0.0
osmnx>=2.0.0
