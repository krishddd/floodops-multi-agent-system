# Core
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
httpx>=0.27.0
python-dotenv>=1.0.0

# LLM (provider-agnostic — both SDKs are lazy-imported; light, pure-Python)
anthropic>=0.40.0
google-genai>=1.0.0

# Google Auth + Workspace
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0

# Observability
prometheus-client>=0.20.0

# Light data libs (manylinux wheels — install fine on slim images)
shapely>=2.0.0
numpy>=1.26.0

# Utilities
python-dateutil>=2.8

# Dev
pytest>=8.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0

# NOTE: heavy GDAL-based geospatial deps (geopandas, osmnx, xarray, dataretrieval)
# live in requirements-geo.txt — they need system GDAL and are only used by the
# raster/vector connectors. The keyless connectors (Open-Meteo, OSM Overpass) use
# httpx only, so the lean image omits the geo stack. The optional semantic-memory
# stack (sentence-transformers + torch) lives in requirements-ml.txt.
