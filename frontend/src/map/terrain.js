/** Terrain tilt configuration. */
export function configureTerrain(map) {
    // Enable tilt and rotation controls
    map.setOptions({ tilt: 45, heading: 0, rotateControl: true, tiltControl: true });
}
